---
trigger: always_on
---

prefer to use % instead of px for containers (% of the screen it needs to fill in, for example)!

- All CSS styles MUST be written in external stylesheet files (e.g. `.css` files). DO NOT use inline style attributes (`style="..."`) in HTML files under any circumstances.
- NEVER use `!important` in CSS unless absolutely necessary. Using `!important` means you are terrible at coding!